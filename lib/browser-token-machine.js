// Generated by CoffeeScript 1.4.0
(function() {
  var repl, spawn;

  repl = require('readline');

  spawn = require('child_process').spawn;

  module.exports = function(url, question, extras, cb) {
    var browsercmd, rl;
    if (typeof extras === 'function') {
      cb = extras;
    }
    browsercmd = (function() {
      switch (process.platform) {
        case 'darwin':
          return 'open';
        case 'linux':
          return 'xdg-open';
      }
    })();
    spawn(browsercmd, [url]);
    if (typeof extras === 'string') {
      console.log(extras);
    }
    rl = repl.createInterface({
      input: process.stdin,
      output: process.stdout
    });
    return rl.question(question, function(thing) {
      cb(thing);
      return rl.close();
    });
  };

}).call(this);
